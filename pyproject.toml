[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build-verbosity = 1
before-test = "CC=gcc CXX=g++ pip install -r {project}/requirements/tests.in"
test-command = "pytest {project}/tests/unit/test_missing.py --no-cov"

[tool.black]
line-length = 100
include = '\.pyi?$'
