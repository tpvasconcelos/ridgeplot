[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
build-verbosity = 1
before-test = "pip install -r {project}/requirements/locked/tests.38-linux.txt"
test-command = "pytest {project}/tests/unit/test_missing.py --no-cov"

[tool.black]
line-length = 100
include = '\.pyi?$'
